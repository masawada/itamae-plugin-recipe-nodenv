RSpec.describe Itamae::Plugin::Recipe::Nodenv do
  it "has a version number" do
    expect(Itamae::Plugin::Recipe::Nodenv::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
